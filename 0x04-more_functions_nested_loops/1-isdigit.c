#include "main.h"
/**
 * _isdigit - Function for upper case
 * @c: parameter of type int
 * Return: Always 0hgg is succes
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
