#include "main.h"
/**
 * mul - function to multiplier two integer
 *
 * @a: first variable
 *
 * @b: variable to tested
 *
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
